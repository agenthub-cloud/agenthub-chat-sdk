# @agenthub/chat

AgentHub 聊天协议 SDK：WebSocket JSON-RPC 传输、Run 事件状态机、渠道工具协议与 REST 封装。

> 协议语义的权威文档在 AgentHub 主仓 `docs/聊天执行引擎.md` 与 `docs/渠道工具与浏览器插件.md`，本 README 只放使用文档。

## 安装

```bash
npm install @agenthub/chat
```

## 最小用法示例

```ts
import { createChatClient } from '@agenthub/chat'

// TODO: Task 10 落地后补全
```
